import React from "react"
import Layout from "../components/Layout"

const X = ({ location }) => {
  return (
    <Layout location={location}>
      <div>X</div>
    </Layout>
  )
}

export default X
