import React from "react"
import Layout from "../components/Layout"

const Faces = ({ location }) => {
  return (
    <Layout location={location}>
      <div>Faces</div>
    </Layout>
  )
}

export default Faces
