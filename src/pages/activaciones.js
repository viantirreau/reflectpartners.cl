import React from "react"
import Layout from "../components/Layout"

const Activaciones = ({ location }) => {
  return (
    <Layout location={location}>
      <div>Activaciones</div>
    </Layout>
  )
}

export default Activaciones
